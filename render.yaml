services:
  - type: web
    name: mcp-python-server
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 mcp-bearer-token/random_connect_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10